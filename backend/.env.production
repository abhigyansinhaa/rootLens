# Production overrides — checked into git (no secrets here)
# Loaded automatically when APP_ENV=production
# All secrets come from Railway Variables — never set them here.
APP_ENV=production
LOG_LEVEL=WARNING
